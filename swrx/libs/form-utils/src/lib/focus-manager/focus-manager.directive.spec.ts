import { FocusManagerDirective } from './focus-manager.directive';

describe('FocusManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusManagerDirective();
    expect(directive).toBeTruthy();
  });
});
