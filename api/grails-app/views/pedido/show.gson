import sx.callcenter.Pedido

model {
	Pedido pedido
}

json g.render(pedido, [ excludes: ['cliente', 'partidas'], resolveTemplate: false]) {
	cliente g.render(pedido.cliente)
	partidas g.render(pedido.partidas)
}
