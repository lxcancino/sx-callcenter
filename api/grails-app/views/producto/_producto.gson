import sx.core.Producto

model {
	Producto producto
}

json g.render(producto, [excludes: ['linea', 'marca', 'clase', 'proveedorFavorito'], resolveTemplate: false]) {
	linea producto.linea?.linea
	marca producto.marca?.marca
	clase producto.clase?.clase
}
